# 大古 OCR Design System

## 1. 设计定位

大古 OCR 是 ZTools 里的即时识别工具。界面服务于三个动作：上传/接收图片、读取结果、继续翻译或复制。视觉风格继承 ZTools 的中性底色、蓝色主操作、低边界噪音和高密度信息结构，避免插件内部再制造一套独立视觉语言。

设计关键词：**高效、轻盈、清晰、现代**。

### 原则

1. **一屏一主任务**：识别页突出图片与结果；设置页只暴露服务与密钥配置。
2. **内容即界面**：不使用装饰性英文标题、重复卡片标题和说明性长文案。
3. **低层级、轻容器**：页面使用浅灰画布 + 白色圆角容器；容器内靠间距与分隔组织层级，不叠加多层边框。
4. **主操作唯一**：每个视图只保留一个实心主按钮，其余操作为次级按钮、图标按钮或文字按钮。
5. **宿主一致**：颜色、圆角、阴影、深浅色跟随 ZTools token；插件只组合语义样式。

## 2. Design Tokens

### 2.1 颜色

| Token | 亮色 | 暗色 | 用途 |
| --- | --- | --- | --- |
| `--bg-color` | `#f4f4f4` | `#303133` | 页面画布 |
| `--surface` | `#ffffff` | `#374151` | 卡片、面板 |
| `--surface-muted` | `#f9fafb` | `#414b56` | 输入区、结果区 |
| `--text-color` | `#333333` | `#f3f4f6` | 主文本 |
| `--text-secondary` | `#616161` | `#bfc0c3` | 说明、标签 |
| `--border-color` | `#e5e7eb` | `#4b5563` | 控件边界 |
| `--divider-color` | `rgba(0,0,0,.08)` | `rgba(255,255,255,.10)` | 分隔线 |
| `--primary-color` | `#0284c7` | `#0ea5e9` | 主按钮、焦点、选中 |
| `--primary-hover` | `#0369a1` | `#38bdf8` | 主操作悬停 |
| `--accent-soft` | `rgba(2,132,199,.10)` | `rgba(14,165,233,.18)` | 选中态、焦点底色 |
| `--success-color` | `#10b981` | `#34d399` | 成功状态 |
| `--warning-color` | `#f59e0b` | `#fbbf24` | 处理中、同步提醒 |
| `--danger-color` | `#ef4444` | `#f87171` | 删除、清空 |

`--primary-color` 支持宿主自定义色覆盖；`theme-blue/purple/green/orange/red/pink` 提供默认色阶映射，局部组件不得硬编码品牌色。

### 2.2 字体与文字

- 字族：`Inter, ui-sans-serif, -apple-system, BlinkMacSystemFont, "Segoe UI", "Microsoft YaHei", sans-serif`。
- 页面基础 `13px / 1.6`，紧凑工具页优先保证可读而不是放大装饰。
- 层级：弹窗标题 `16px / 650`；分区标签 `12px / 600`；正文 `13px`；辅助说明 `11-12px`；输入文本 `14px / 1.65`。
- 禁止英文装饰标题（如 `TRANSLATE`、`OCR RESULT`、`HISTORY`）。技术名词保留在控件值或选项文本中，例如“OCR 服务”。

### 2.3 几何、阴影与动效

| Token | 值 | 用途 |
| --- | --- | --- |
| `--radius-sm` | `8px` | 输入框、小按钮 |
| `--radius-md` | `12px` | 卡片、工具组 |
| `--radius-lg` | `16px` | 弹窗、浮动工具栏 |
| `--radius-full` | `999px` | 状态点、开关 |
| `--space-1..6` | `4/8/12/16/20/24px` | 主要间距 |

控件最小高度：按钮 `34px`，输入框 `36px`，工具栏图标 `34-38px`。阴影分两级：`--shadow-card` 用于页面卡片，`--shadow-pop` 用于弹窗与浮层。动效只对颜色、边界、阴影、透明度和轻微 transform 做 `150-200ms ease`，禁止常驻装饰动画。

## 3. 通用组件

### 按钮

| 类型 | 用法 | 样式 |
| --- | --- | --- |
| Primary | 当前视图唯一主操作 | 实心 `--primary-color`，白字，`34px` |
| Secondary | 常用并列操作 | 表面底色 + 1px 边界 |
| Ghost | 清空、关闭等低权重操作 | 透明底色，悬停才显色 |
| Text | 面板内轻操作 | 无底色，次级文字色 |
| Icon | 工具栏、弹窗关闭 | 正方形，图标 `18-20px` |

焦点态统一为 `2px --focus-ring` 加 `2px` 偏移。禁用态降低对比，不改变布局。

主题色只用于主操作与 focus 态；状态提示语、文字按钮等次级入口使用 `--text-secondary`，避免主色在页面里到处出现。

### 输入与卡片

- 输入框和下拉框统一 `36px`、`--surface-muted`、`--radius-sm`。
- focus 使用主色边界 + 3px accent 光环；表单标签在上，说明在下。
- 文本域最小高度 `132px`，结果区可拉伸，均只允许垂直 resize。
- 卡片只承载一个任务块，不嵌套卡片；没有状态时不渲染卡片标题。
- 卡片内分隔使用 `--divider-color`，不叠加第二层边框。

### 弹窗与状态

- 设置弹窗宽度 `min(100%, 760px)`；历史弹窗 `min(100%, 560px)`。
- 弹窗采用 header / scroll body / footer 三段结构；header 只一个标题和一个关闭操作。
- footer 右对齐主操作；破坏性操作使用危险文字按钮。
- `busy` 用右下浮动胶囊；`status` 固定在工具条左侧，避免布局跳动；空状态只要一句话和格式说明。

## 4. 页面规格

### 4.1 翻译输入

- 结构：文本输入 → 语言控制 → 主操作 → 结果。
- 删除“输入要翻译的文字”标题与英文装饰；源/目标语言与翻译按钮合并到一行。
- 结果出现时只显示轻量标签“译文”和复制操作。

### 4.2 OCR 工作区

- 上传区使用虚线容器、居中图标和一句话说明，支持点击、拖入、粘贴。
- 图片/结果双栏：图片与文字框等宽（1:1）且图片容器与 OCR 输入框上下对齐、高度一致，图列标题行放“预览”“复制”“编辑”入口，结果列放语言控制、可编辑文本和动作；预览框内滚轮以光标为锚点放大、按住拖动平移，缩小回原始倍数复位。
- 翻译时预览默认收起，用户可展开；原文与译文双栏对比。
- 结果只保留“原文”“译文”标签；“可编辑”通过 placeholder 表达。
- 操作顺序：**复制 → 重新识别 → 翻译 → 清空**；原文与译文各自在标题行右上角提供复制入口，译文复制只复制译文。
- 结果页不设独立底部工具条：状态提示与历史/配置并入结果操作行右端，操作栏横跨图片与结果两栏、贴在两容器底部；上传/翻译输入页保留一条轻量工具条，不展示“最近识别”大标题。

### 4.3 设置

- 弹窗标题为“配置”，body 分为“服务”和“内置服务密钥”两个轻卡片。
- OCR、翻译、源语言、目标语言合并到同一服务卡片，形成一次扫描即可完成的表单。
- 内置密钥按服务字段分组；同步密钥风险提示压缩为开关和两行说明。
- footer：左侧测试当前 OCR，右侧保存设置。

### 4.4 截图与图片编辑

截图与图片编辑共用同一套界面与实现：**沉浸式覆盖层**负责冻结画面与框选，**图片模式**直接打开待编辑图片（`编辑图片` 与采集失败兜底）。

- 沉浸式覆盖层铺满光标所在显示器，冻结画面即画布，不使用深色背景容器；选区用蓝色边框、八个白色手柄与 `宽 × 高 px` 尺寸标签表达，选区外加深遮罩。
- 工具条为浅色圆角面板（圆角 `10px`、轻投影），贴靠选区浮动：默认在选区下方，空间不足时换到上方；子工具条在主工具条下方展开。
- 工具分组：`选择/形状/线条/画笔/荧光笔/马赛克/文字/橡皮擦`、`撤销/重做`、`OCR/翻译`、`取消/固定/保存/复制`。当前工具与复制按钮使用主色填充，图标统一取自 Lucide（ISC）。
- 图片模式沿用同一工具条：没有选区时工具条常驻底部居中，出现选区后贴靠选区浮动。
- 主页面与截图工具条共用同一套 token 与 Lucide（ISC）图标；主页面窗口统一 `800×600`（4:3），底部工具条贴底，内容在页面内滚动。
- 样式面板保持锚定工具栏的轻量 popover，按颜色、粗细、字号分组；沉浸式覆盖层的马赛克支持粒度调节。

## 5. 主题接入

- 页面启动时同步 ZTools `getThemeInfo()`，并把 `isDark`、`primaryColor`、`windowMaterial` 写到根节点。
- 无宿主 API 时回退到 `prefers-color-scheme`。
- 监听 `onThemeChange()`，运行时更新根节点类与 `--primary-color`。
- 新颜色先进入本文件，再进入 `plugin/src/theme.css`。

## 6. 验收标准

1. 主页面、翻译输入、OCR 结果、设置、历史均无英文装饰标题与重复大标题。
2. 每个可见视图最多一个实心主按钮；“历史/配置”以轻量工具形式常驻。
3. 明暗主题切换后画布、文本、边界、控件保持足够对比。
4. 截图工具栏底部居中、可呼吸；工具、快捷键和 OCR/翻译入口保留。
5. Vitest 与 Playwright 全部通过；功能选择器与 IDs 不因视觉重构丢失。


